{
  "name": "portfolio-framer-mar26",
  "private": true,
  "description": "Root workspace: shared Playwright E2E across repo packages (e.g. aurora).",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
