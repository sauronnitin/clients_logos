"use client"

import { AuroraBackground } from "@/components/ui/aurora-background"

/** Aurora only — add your own hero copy as children where you use `AuroraBackground`. */
export function AuroraBackgroundDemo() {
  return <AuroraBackground />
}
